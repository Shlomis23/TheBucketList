# placeholders בלבד — לעולם לא להכניס ערכים אמיתיים לקובץ הזה
# ולעולם לא להוסיף קידומת NEXT_PUBLIC_ לסוד (spec סעיף 9.3).

# --- משתני לקוח (בטוחים לחשיפה, מוגנים ע"י RLS) ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# --- סודות שרת (לא להעלות לצ'אט, לא ל-repo; רק ב-Environment Variables
#     של Vercel/סביבת ה-deploy) ---
# SUPABASE_SERVICE_ROLE_KEY=
# INVITE_COOKIE_SECRET=
